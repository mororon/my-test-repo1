name: Change shell
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - shell: bash   #シェルをbashに変更
        run: echo "${SHELL}"
      - shell: python  #シェルにPythonを指定
        run: |
          import os
          print(os.environ['PATH'])
